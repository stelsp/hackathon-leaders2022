import React from "react";
import NavBar from "../components/Common/NavBar";

const Header = () => {
  return (
    <header>
      <NavBar place="header" />
    </header>
  );
};

export default Header;
